apiVersion: v1
kind: Service
metadata:
  name: pos
  namespace: pos
spec:
  selector:
    app: pos

  type: LoadBalancer
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
